{%- macro sort(route, name, field) %}
    {%- set sort = app.request.session.get('filter_sort.' ~ name) -%}
    {%- if sort and sort.field == field -%}
        {%- if sort.direction == 'DESC' -%}
            <a href="{{ path(route, {field: field, direction: 'ASC'}) }}" class="float-right d-print-none">
                <i class="fas fa-sort-down"></i>
            </a>
        {%- elseif sort.direction == 'ASC' -%}
            <a href="{{ path(route, {field: field, direction: 'DESC'}) }}" class="float-right d-print-none">
                <i class="fas fa-sort-up"></i>
            </a>
        {%- endif -%}
    {%- else -%}
        <a href="{{ path(route, {field: field}) }}" class="float-right text-muted d-print-none">
            <i class="fas fa-sort"></i>
        </a>
    {%- endif -%}
{% endmacro -%}
